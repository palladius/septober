<%
 #Todo.with_scope(:find => {:conditions => "user_id = #{current_user.id}"},
 #                :create => {:user_id => current_user.id}) do
 # user_posts = Post.find(:all)
%>
<%# title "Todos" %>

<%=render :partial => "projects/upright_links" %>

<table>
  <tr>
    <th>Todos</th>
    <th>Project</th>
  </tr>
  <% for todo in @todos %>
    <tr>
			
      <td><%= render_priority_icon(todo.priority) %></td>
      <td><%= link_to(render_todo_name(todo) , (todo) ) %></td>
<%#= render_project(todo.project) %>
      <td>
          <%#= link_to "Show", todo %>
          <%= iconed_link_to "Edit", edit_todo_path(todo) %>
          <%= iconed_link_to "Destroy", todo, :confirm => 'Are you sure?', :method => :delete %>
      </td>
      <td><%#= link_to "Toggle (TODO)", todo, :method => :toggle %></td>
    </tr>
  <% end %>
</table>

<p><%= link_to "New Todo", new_todo_path(todo) %></p>
